# Fixtures

Public fixtures here are sanitized and reviewable.

Do not commit private captures, secrets, raw local env data, GPS coordinates, real names, or unreviewed payloads.

Use `Fixtures/private/` for local-only captures; it is ignored by Git.
